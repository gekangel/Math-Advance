function new_population =
regeneration(children, population) 

fitnes = zeros(1, length(population));


for i=1:length(fitnes)
    fitnes(i)= population(i).fitnes;
end 

for i=1:length(children)
    [~,index]= min(fitnes);
    %remove member
    population (index)=[];
    fitnes(index)=[];
end 

%new member
for i=1:length(children)
    n=length(population) +1;
    population(n) = children(1);
end
new_population = 